[pytest]

log_cli = true
log_cli_level = DEBUG
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %H:%M:%S

pythonpath = .

markers =
    api: API autotests
    api_version(version): API version marker (e.g., 'teamcity')
    ui: UI autotests
    test: Debug of local tests